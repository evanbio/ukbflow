% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_write}
\alias{audit_write}
\title{Write a ukbflow audit manifest}
\usage{
audit_write(audit, file = "ukbflow-audit.json", overwrite = FALSE)
}
\arguments{
\item{audit}{A \code{ukbflow_audit} object created by
\code{\link{audit_start}}.}

\item{file}{(character) Output JSON file path. Default:
\code{"ukbflow-audit.json"}.}

\item{overwrite}{(logical) Overwrite \code{file} if it already exists.
Default: \code{FALSE}.}
}
\value{
Invisibly returns the normalized output path.
}
\description{
Writes a \code{ukbflow_audit} object to a JSON manifest. The manifest is a
plain-list representation of the audit object: session information is
converted to character lines, and record layers such as \code{extraction}
and \code{snapshots} are written as JSON arrays.
}
\examples{
aud <- audit_start("example_analysis")
outfile <- tempfile(fileext = ".json")
audit_write(aud, outfile)
}
