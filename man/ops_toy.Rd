% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{ops_toy}
\alias{ops_toy}
\title{Generate toy UKB-like data for testing and development}
\usage{
ops_toy(scenario = "cohort", n = 1000L, seed = 42L)
}
\arguments{
\item{scenario}{(character) Data structure to generate:
\itemize{
\item \code{"cohort"}: wide participant-level table for the full
\verb{derive_*} → \verb{assoc_*} → \verb{plot_*} pipeline.
\item \code{"forest"}: association results table matching \code{assoc_coxph()} output,
for testing \code{plot_forest()}. \code{n} = number of exposures (default 8).
}}

\item{n}{(integer) Number of participants. Default \code{1000L}.}

\item{seed}{(integer or NULL) Random seed for reproducibility. Pass \code{NULL}
for a different dataset on every call. Default \code{42L}.}
}
\value{
A \code{data.table} with UKB-style column names. See Details for the
columns included in each scenario.
}
\description{
Creates a small, synthetic dataset that mimics the structure of UK Biobank
phenotype data on the RAP. Useful for developing and testing \verb{derive_*},
\verb{assoc_*}, and \verb{plot_*} functions without requiring real UKB data access.
}
\details{
This dataset is entirely synthetic. Column names follow RAP conventions
(e.g. \code{p41270}, \code{p20002_i0_a0}).
\subsection{scenario = "cohort"}{

Includes the following column groups:
\itemize{
\item \strong{Demographics}: \code{eid}, \code{p31}, \code{p34}, \code{p53_i0}, \code{p21022}
\item \strong{Covariates}: \code{p21001_i0}, \code{p20116_i0}, \code{p1558_i0}, \code{p21000_i0},
\code{p22189}, \code{p54_i0}
\item \strong{Genetic PCs}: \code{p22009_a1} – \code{p22009_a10}
\item \strong{Self-report disease}: \verb{p20002_i0_a0–a4}, \verb{p20008_i0_a0–a4}
\item \strong{HES}: \code{p41270} (JSON array), \verb{p41280_a0–a9}
\item \strong{Cancer registry}: \code{p40006_i0}, \code{p40005_i0}
\item \strong{Death registry}: \code{p40001_i0}, \verb{p40002_i0_a0–a1}, \code{p40000_i0}
\item \strong{First occurrence}: \code{p131742}
\item \strong{Messy columns}: \code{messy_allna}, \code{messy_empty}, \code{messy_label}
}
}
}
\examples{
\dontrun{
dt <- ops_toy()
dt <- ops_toy(n = 500, seed = 1)

# Dynamic dataset (different every call)
dt <- ops_toy(seed = NULL)

# Feed directly into derive pipeline
dt <- ops_toy()
dt <- derive_missing(dt)
}
}
