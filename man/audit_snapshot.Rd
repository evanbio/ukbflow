% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_snapshot}
\alias{audit_snapshot}
\title{Record a data snapshot in a ukbflow audit object}
\usage{
audit_snapshot(
  audit,
  data = NULL,
  label = NULL,
  reset = FALSE,
  check_na = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{audit}{A \code{ukbflow_audit} object created by
\code{\link{audit_start}}.}

\item{data}{A data.frame or data.table to snapshot. Required unless
\code{reset = TRUE}.}

\item{label}{(character) A unique label for this snapshot, e.g.
\code{"raw_extracted"} or \code{"analysis_ready"}. Required unless
\code{reset = TRUE}.}

\item{reset}{(logical) If \code{TRUE}, clears only the
\code{audit$snapshots} layer and returns the updated audit object.
Default: \code{FALSE}.}

\item{check_na}{(logical) Whether to count columns with any \code{NA} or
blank string values. Set to \code{FALSE} to avoid scanning large datasets.
Default: \code{TRUE}.}

\item{verbose}{(logical) Print a short status message. Default:
\code{TRUE}.}
}
\value{
The updated \code{ukbflow_audit} object.
}
\description{
Captures a lightweight structural snapshot of a data.frame at a named
analysis stage and appends it to the \code{snapshots} layer of a
\code{ukbflow_audit} object. This function mirrors the core behavior of
\code{\link{ops_snapshot}} but stores records inside the explicit audit
object rather than a session cache.
}
\examples{
aud <- audit_start("example_analysis")
dt <- data.frame(eid = 1:3, x = c(1, NA, 3))
aud <- audit_snapshot(aud, dt, "raw")
}
